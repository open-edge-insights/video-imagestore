{
  "definitions": {},
  "type": "object",
  "required": [
    "minio"
  ],
  "properties": {
    "minio": {
      "type": "object",
      "required": [
        "retentionTime",
        "retentionPollInterval",
        "ssl"
      ],
      "properties": {
        "retentionTime": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "retentionPollInterval": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "ssl": {
          "type": "string",
          "pattern": "^(.*)$"
        }
      }
    }
  }
}